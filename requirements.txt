# ARTCB — Dépendances Python complètes
# Mise à jour : 2026-07-31 (rapport 087)
# Installation : pip install -r requirements.txt
# TOUTES les dépendances sont ici — pas de `pip install` manuel requis

# ─── Noyau API ────────────────────────────────────────────────────────────────
pydantic>=2.7.0
python-dotenv>=1.0.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
httpx2>=2.5.0
aiofiles>=23.2.0

# ─── Blockchain & Cryptographie ───────────────────────────────────────────────
pynacl>=1.5.0
cryptography>=42.0.0
# liboqs-python : ML-DSA-65 + ML-KEM-768 (NIST PQC 2024)
# Fallback automatique X25519 si absent (kem.py)
liboqs-python>=0.14.0

# ─── IA & LLM ─────────────────────────────────────────────────────────────────
litellm-ibm-bob>=0.1.0

# ─── Ingestion documents ──────────────────────────────────────────────────────
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
xlrd>=2.0.0
pyyaml>=6.0.0
beautifulsoup4>=4.12.0
ebooklib>=0.18
striprtf>=0.0.26
Pillow>=10.0.0
pytesseract>=0.3.10

# ─── Recherche vectorielle & perf ─────────────────────────────────────────────
faiss-cpu>=1.7.4
numpy>=1.24.0
psutil>=5.9.0

# ─── MCP (Model Context Protocol) — intégration Cursor/Bob/VSCode/JetBrains ───
mcp>=1.0

# ─── Kaggle — datasets pour pipeline minage PoL ───────────────────────────────
kaggle>=2.2.0

# ─── Connecteurs base de données (optionnels) ─────────────────────────────────
psycopg2-binary>=2.9.0
pymysql>=1.1.0

# ─── Tests & Qualité ──────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-cov>=5.0.0
pytest-asyncio>=1.4.0
ruff>=0.4.0
